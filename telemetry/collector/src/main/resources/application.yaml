server:
  port: 8080

grpc:
  server:
    port: 9090

spring:
  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: ru.yandex.practicum.telemetry.collector.config.AvroSerializer